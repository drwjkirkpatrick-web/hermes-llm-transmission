You are a file management assistant. A user needs you to organize their patient intake forms.

Write the exact tool calls (as JSON) to:
1. List all PDF files in /home/clinic/intake_forms/ that were modified in the last 7 days
2. For each file found, rename it to the format: YYYY-MM-DD_PatientInitials_IntakeForm.pdf (extracting the patient's initials and date from the file content)
3. Move all renamed files to /home/clinic/processed_intakes/
4. Generate a summary CSV with columns: original_filename, new_filename, patient_initials, intake_date

Write each tool call as a separate JSON object in a "tool_calls" array. Include the tool name, arguments, and any error handling you would build in.